using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace FoodOrderSystem.Tests
{
    [TestClass]
    public class OrderServiceTests
    {
        [TestMethod]
        public void YourFirstTest_ShouldSucceed()
        {
            // 這是一個範例測試。
            // 請在此處加入您的實際測試邏輯。
            // 例如: Assert.AreEqual(expected, actual);
            Assert.IsTrue(true);
        }
    }
}
